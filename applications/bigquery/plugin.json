{
  "version": "1",
  "type": "MpasApplicationPlugin",
  "pluginDid": "did:web:wivity.com:plugins:bigquery-mcp-server",
  "pluginVersion": "0.1.0",
  "publisherDid": "did:web:wivity.com",
  "applicationDid": "did:web:wivity.com:applications:bigquery-mcp-server",
  "executionProfile": {
    "id": "did:web:profiles.oma3.org:mcp",
    "format": "mcp.toolsCall",
    "protocolVersion": "2024-11-05"
  },
  "credentialRequirements": [
    {
      "type": "googleCloudBigQueryCredential",
      "requiredCapabilities": [
        "bigquery.jobs.create",
        "bigquery.datasets.get",
        "bigquery.tables.get",
        "bigquery.tables.getData",
        "bigquery.tables.create",
        "bigquery.tables.updateData",
        "dataplex.catalogEntries.search",
        "cloudaicompanion.companions.generateChat"
      ],
      "description": "Google Cloud credential scoped to the intended project and datasets with least-privileged BigQuery, Dataplex catalog, and optional Conversational Analytics permissions. Access tokens, service-account keys, query results, and dataset contents must never be disclosed outside the authorized action result."
    }
  ],
  "operations": {
    "analyze_contribution": {
      "description": "Use this tool to analyze the contribution about changes to key metrics in multi-dimensional data.",
      "impact": "high",
      "executionPayloadSchema": {
        "type": "object",
        "required": [
          "name",
          "arguments"
        ],
        "properties": {
          "name": {
            "const": "analyze_contribution"
          },
          "arguments": {
            "type": "object",
            "properties": {
              "contribution_metric": {
                "type": "string",
                "description": "The name of the column that contains the metric to analyze.\n\t\tProvides the expression to use to calculate the metric you are analyzing.\n\t\tTo calculate a summable metric, the expression must be in the form SUM(metric_column_name),\n\t\twhere metric_column_name is a numeric data type.\n\t\tTo calculate a summable ratio metric, the expression must be in the form\n\t\tSUM(numerator_metric_column_name)/SUM(denominator_metric_column_name),\n\t\twhere numerator_metric_column_name and denominator_metric_column_name are numeric data types.\n\t\tTo calculate a summable by category metric, the expression must be in the form\n\t\tSUM(metric_sum_column_name)/COUNT(DISTINCT categorical_column_name). The summed column must be a numeric data type.\n\t\tThe categorical column must have type BOOL, DATE, DATETIME, TIME, TIMESTAMP, STRING, or INT64."
              },
              "dimension_id_cols": {
                "type": "array",
                "description": "An array of column names that uniquely identify each dimension.",
                "items": {
                  "type": "string",
                  "description": "A dimension column name."
                }
              },
              "input_data": {
                "type": "string",
                "description": "The data that contain the test and control data to analyze. Can be a fully qualified BigQuery table ID or a SQL query."
              },
              "is_test_col": {
                "type": "string",
                "description": "The name of the column that identifies whether a row is in the test or control group."
              },
              "pruning_method": {
                "type": "string",
                "description": "The method to use for pruning redundant insights. Can be 'NO_PRUNING' or 'PRUNE_REDUNDANT_INSIGHTS'.",
                "default": "PRUNE_REDUNDANT_INSIGHTS"
              },
              "top_k_insights_by_apriori_support": {
                "type": "integer",
                "description": "The number of top insights to return, ranked by apriori support.",
                "default": 30
              }
            },
            "required": [
              "input_data",
              "contribution_metric",
              "is_test_col"
            ]
          }
        },
        "additionalProperties": false
      }
    },
    "ask_data_insights": {
      "description": "Use this tool to perform data analysis, get insights,\nor answer complex questions about the contents of specific\nBigQuery tables.\n",
      "impact": "high",
      "executionPayloadSchema": {
        "type": "object",
        "required": [
          "name",
          "arguments"
        ],
        "properties": {
          "name": {
            "const": "ask_data_insights"
          },
          "arguments": {
            "type": "object",
            "properties": {
              "table_references": {
                "type": "string",
                "description": "A JSON string of a list of BigQuery tables to use as context. Each object in the list must contain 'projectId', 'datasetId', and 'tableId'. Example: '[{\"projectId\": \"my-gcp-project\", \"datasetId\": \"my_dataset\", \"tableId\": \"my_table\"}]'."
              },
              "user_query_with_context": {
                "type": "string",
                "description": "The user's question, potentially including conversation history and system instructions for context."
              }
            },
            "required": [
              "user_query_with_context",
              "table_references"
            ]
          }
        },
        "additionalProperties": false
      }
    },
    "execute_sql": {
      "description": "Use this tool to execute sql statement.",
      "impact": "critical",
      "executionPayloadSchema": {
        "type": "object",
        "required": [
          "name",
          "arguments"
        ],
        "properties": {
          "name": {
            "const": "execute_sql"
          },
          "arguments": {
            "type": "object",
            "properties": {
              "dry_run": {
                "type": "boolean",
                "description": "If set to true, the query will be validated and information about the execution will be returned without running the query. Defaults to false.",
                "default": false
              },
              "sql": {
                "type": "string",
                "description": "The SQL to execute."
              }
            },
            "required": [
              "sql"
            ]
          }
        },
        "additionalProperties": false
      }
    },
    "forecast": {
      "description": "Use this tool to forecast time series data.",
      "impact": "high",
      "executionPayloadSchema": {
        "type": "object",
        "required": [
          "name",
          "arguments"
        ],
        "properties": {
          "name": {
            "const": "forecast"
          },
          "arguments": {
            "type": "object",
            "properties": {
              "data_col": {
                "type": "string",
                "description": "The name of the time series data column."
              },
              "history_data": {
                "type": "string",
                "description": "The table id or the query of the history time series data."
              },
              "horizon": {
                "type": "integer",
                "description": "The number of forecasting steps.",
                "default": 10
              },
              "id_cols": {
                "type": "array",
                "description": "An array of the time series id column names.",
                "items": {
                  "type": "string",
                  "description": "The name of time series id column."
                },
                "default": []
              },
              "timestamp_col": {
                "type": "string",
                "description": "The name of the time series timestamp column."
              }
            },
            "required": [
              "history_data",
              "timestamp_col",
              "data_col"
            ]
          }
        },
        "additionalProperties": false
      }
    }
  }
}
