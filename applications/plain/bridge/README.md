# mpas-bridge-plain

MPAS bridge MCP server for **plain**, generated by `bridge-generator` from upstream `plain-mcp`.

Tools: list_help_centers, get_help_center, create_help_center, update_help_center, delete_help_center, update_help_center_custom_domain, verify_help_center_custom_domain, list_help_center_articles, get_help_center_article, get_help_center_article_by_slug, upsert_help_center_article, delete_help_center_article, generate_help_center_article, list_help_center_article_groups, get_help_center_article_group, create_help_center_article_group, update_help_center_article_group, delete_help_center_article_group, get_help_center_index, update_help_center_index, create_knowledge_source, delete_knowledge_source, upsert_customer, delete_customer, mark_customer_as_spam, unmark_customer_as_spam, get_customer, create_customer_event, create_thread, assign_thread, unassign_thread, change_thread_priority, update_thread_title, delete_thread, create_thread_event, list_label_types, create_label_type, add_labels_to_thread, remove_labels_from_thread, upsert_company, delete_company, upsert_tenant, delete_tenant, list_snippets, create_snippet, update_snippet, delete_snippet, list_webhooks, create_webhook, update_webhook, delete_webhook, list_users, get_workspace, send_email, send_chat, list_autoresponders, create_autoresponder, delete_autoresponder, list_tiers, create_tier, delete_tier, list_customer_groups, create_customer_group, add_customer_to_groups, remove_customer_from_groups, list_thread_field_schemas, upsert_thread_field, delete_note, list_threads, get_thread, add_labels, remove_labels, download_attachment, search_customers, get_queue_stats, reply_to_thread, mark_thread_done, mark_thread_todo, change_thread_status_to_todo, snooze_thread, create_note, link_thread_to_linear

The runtime in `src/index.ts` loads the verbatim discovered tool surface from `src/tools.json`. The build copies both into `dist/`; keep `dist/index.js` and `dist/tools.json` together when packaging or deploying the bridge.

## Usage

```sh
npm install
npm run build
node dist/index.js --config <path-to-bridge-config.json>
```

The bridge config format matches the MPAS demo proposer bridge (plugin path, adapter URL, agent key, coordination URL, and workflow storage). The server auto-detects MCP 2026-07-28 Tasks clients and conventional MCP clients that need the MPAS wait-tool compatibility surface. All tool calls are routed through MPAS: the bridge signs an Action Package and submits it to the configured Credential Adapter; nothing is proxied directly to the upstream server.

One bridge serves exactly one MCP client or agent identity and holds one private key for one proposer DID. Do not share a bridge process or key across independent clients; deploy a separate bridge instance and key for each agent.

This file is generated then checked in. Edit freely; regeneration preserves files listed in `.generator-keep`.
