{
  "name": "mpas-bridge-fastly",
  "version": "0.1.0",
  "description": "MPAS bridge MCP server for fastly (generated by bridge-generator)",
  "type": "module",
  "bin": {
    "mpas-bridge-fastly": "./dist/index.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && node -e \"require('node:fs').copyFileSync('src/tools.json', 'dist/tools.json')\"",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.13.0",
    "@oma3/mpas": "^0.1.0-alpha.4"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22"
  }
}
