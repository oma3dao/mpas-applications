{
  "name": "mpas-bridge-stripe",
  "version": "0.1.0",
  "description": "MPAS bridge MCP server for stripe (generated by bridge-generator)",
  "type": "module",
  "bin": {
    "mpas-bridge-stripe": "./dist/index.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && node -e \"require('node:fs').copyFileSync('src/tools.json', 'dist/tools.json')\"",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "@oma3/mpas": "0.1.0-alpha.11"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22"
  }
}
