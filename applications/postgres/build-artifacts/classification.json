{
  "version": "1",
  "type": "ImpactClassificationDraft",
  "draft": false,
  "operations": {
    "query": {
      "impact": "high",
      "rationale": "Read-only by construction (the server wraps it in BEGIN TRANSACTION READ ONLY), but it is the only tool this server exposes. Governed deliberately: an empty governed surface would leave nothing for MPAS to do here. Arbitrary SQL across a whole database is a far broader read than a scoped metadata getter."
    }
  }
}
