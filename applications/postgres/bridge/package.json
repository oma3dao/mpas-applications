{
  "name": "mpas-bridge-postgres",
  "version": "0.1.0",
  "description": "MPAS bridge MCP server for postgres (generated by bridge-generator)",
  "type": "module",
  "bin": {
    "mpas-bridge-postgres": "./dist/index.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json && node -e \"require('node:fs').copyFileSync('src/tools.json', 'dist/tools.json')\"",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "@oma3/mpas": "0.1.0-alpha.12"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=22"
  }
}
