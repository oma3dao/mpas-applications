# mpas-bridge-railway

For Railway credential setup, resource targeting, and provider-specific
security guidance, see the [Railway application README](../README.md).

MPAS bridge MCP server for **railway**, generated by `bridge-generator` from upstream `railway`.

Tools: add_reference_variable, connect_service_source, create_bucket, create_environment, create_project, create_service, create_tcp_proxy, create_volume, delete_domain, deploy, deploy_template, disconnect_service_source, docs_fetch, docs_search, domain_status, environment_status, generate_domain, get_logs, get_service_config, get_tcp_proxy, http_error_rate, http_requests, http_response_time, link_environment, link_service, list_deployments, list_domains, list_projects, list_services, list_tcp_proxies, list_variables, list_workspaces, private_network_status, private_network_update, remove_bucket, remove_service, remove_tcp_proxy, remove_volume, retry_domain_certificate, scale_service, search_templates, service_metrics, set_variables, update_domain, update_service, update_volume, whoami

The runtime in `src/index.ts` loads the verbatim discovered tool surface from `src/tools.json`. The build copies both into `dist/`; keep `dist/index.js` and `dist/tools.json` together when packaging or deploying the bridge.

## Usage

```sh
npm install
npm run build
node dist/index.js --config <path-to-bridge-config.json>
```

The bridge config format matches the MPAS demo proposer bridge (plugin path, adapter URL, agent key, coordination URL, approval strategy). All tool calls are routed through the MPAS protocol: the bridge signs an Action Package and submits it to the configured Credential Adapter; nothing is proxied directly to the upstream server.

This file is generated then checked in. Edit freely; regeneration preserves files listed in `.generator-keep`.
